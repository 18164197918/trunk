import { classPrefix } from '../common/constant'

export default {
  filters: {
    className (name) {
      if (name) {
        return classPrefix + name
      } else {
        return ''
      }
    }
  }
}
